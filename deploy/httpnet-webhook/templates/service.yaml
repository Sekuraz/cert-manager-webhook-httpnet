apiVersion: v1
kind: Service
metadata:
  name: {{ include "httpnet-webhook.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "httpnet-webhook.name" . }}
    chart: {{ include "httpnet-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "httpnet-webhook.name" . }}
    release: {{ .Release.Name }}
